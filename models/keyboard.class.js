/**
 * Keyboard class - stores the status of all pressed keys
 */
class Keyboard extends MovableObject {
    LEFT;    // Left arrow key
    RIGHT;   // Right arrow key
    UP;      // Up arrow key
    DOWN;    // Down arrow key
    SPACE;   // Spacebar (jump)
    D;       // D key (throw bottles)
}